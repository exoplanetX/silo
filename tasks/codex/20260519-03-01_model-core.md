

~~~markdown
# Codex Task 20260519-03-01: Complete Model Core

## Goal

Advance SILO from a project scaffold to a stable Phase 1 model-core implementation.

This task should complete the basic modeling layer needed to express small LP/MIP models through Python objects and JSON files, validate model consistency, and produce a deterministic canonical form suitable for later LP simplex implementation.

Repository:

https://github.com/exoplanetX/silo

## Required Task File

Save this task prompt as:

```text
tasks/codex/20260519-03-01_model-core.md
~~~

Do not modify existing files under `tasks/codex/`.

This task may update:

```text
tasks/phases/phase_01_model_core.md
```

only if the phase description needs minor alignment with the implementation.

Do not change the task-directory rules unless explicitly needed. If task rules need to change, update `tasks/README.md` in the same commit.

------

# Part 1: Scope

Complete Phase 1 around the following modules:

```text
src/silo/core/
src/silo/modeling/
src/silo/io/
tests/unit/
tests/fixtures/
examples/
```

The focus is:

1. model validation;
2. linear expression support;
3. JSON input;
4. solution JSON output;
5. canonical form enhancement;
6. deterministic tests.

Do not implement simplex, revised simplex, presolve, branch-and-bound, cuts, decomposition, stochastic programming, robust optimization, or native code in this task.

------

# Part 2: Model Validation

Add model validation logic.

Recommended location:

```text
src/silo/core/model.py
```

Add a method:

```python
def validate(self) -> None:
    ...
```

The method should check at least:

1. variable names are unique;
2. constraint names are unique;
3. all constraint coefficients refer to known variables;
4. all objective coefficients refer to known variables;
5. all bounds are valid;
6. binary variables have bounds compatible with `[0, 1]`;
7. integer variables have valid finite or infinite bounds;
8. empty variable names are rejected;
9. empty constraint names are rejected;
10. objective sense is valid.

Keep validation deterministic and readable.

Do not introduce complex modeling conventions yet.

------

# Part 3: Core Object Improvements

Review and improve these files if needed:

```text
src/silo/core/bounds.py
src/silo/core/variable.py
src/silo/core/constraint.py
src/silo/core/objective.py
src/silo/core/model.py
src/silo/core/solution.py
```

Expected behavior:

- `Bounds.validate()` should reject lower > upper.
- `Variable` should reject empty names.
- `Constraint` should reject empty names.
- `Model.add_variable()` should reject duplicate variable names.
- `Model.add_constraint()` should reject duplicate constraint names.
- `Model.validate()` should catch cross-object errors such as unknown variable references.

Do not make `core` depend on `lp`, `mip`, `cuts`, `decomposition`, or `uncertainty`.

------

# Part 4: Linear Expression System

Enhance:

```text
src/silo/modeling/expressions.py
```

Implement a small readable `LinearExpression` class that supports:

```python
expr = LinearExpression({"x": 2.0, "y": 3.0}, constant=1.0)
```

Required methods or behaviors:

1. deterministic coefficient aggregation;
2. addition of expressions;
3. subtraction of expressions;
4. scalar multiplication;
5. coefficient lookup;
6. zero-coefficient cleanup if practical;
7. conversion to a coefficient dictionary.

Keep this simple. Do not build a full algebraic modeling language yet.

Add tests for:

```text
2x + 3x = 5x
expr1 + expr2
expr1 - expr2
2 * expr
coefficient lookup
constant handling
```

------

# Part 5: Canonical Form Enhancement

Enhance:

```text
src/silo/modeling/canonical_form.py
```

The current canonical form is a dense placeholder. Keep it dense for now, but include enough information for future LP/MIP phases.

The canonical form should include:

```python
variable_names
constraint_names
objective_coefficients
objective_constant
constraint_matrix
rhs
constraint_senses
objective_sense
variable_lower_bounds
variable_upper_bounds
variable_types
```

Required behavior:

1. preserve model variable order;
2. preserve model constraint order;
3. fill missing coefficients with 0.0;
4. include objective constant;
5. include variable bounds;
6. include variable types;
7. call `model.validate()` before conversion;
8. use immutable tuples in the returned dataclass.

Do not yet convert all rows into row-bound form. However, leave a clear note or TODO that later phases may move toward:

```text
row_lower_bounds <= A x <= row_upper_bounds
```

------

# Part 6: JSON Reader

Improve:

```text
src/silo/io/json_reader.py
```

The JSON reader should load a model from the fixture format.

Required behavior:

1. load variables;
2. load lower/upper bounds;
3. treat `null` upper bound as infinity;
4. load variable types: continuous, integer, binary;
5. load objective sense;
6. load objective coefficients;
7. load objective constant if present;
8. load constraints;
9. call `model.validate()` before returning;
10. raise clear errors for invalid enum values or invalid structures.

Keep the JSON format simple and close to the existing fixtures.

Add tests for:

```text
production.json
diet.json
knapsack.json
invalid unknown variable
invalid binary bounds
invalid duplicate variable
```

------

# Part 7: Solution Writer

Improve or verify:

```text
src/silo/io/solution_writer.py
```

Required behavior:

1. write solution status;
2. write objective value;
3. write primal values;
4. write dual values;
5. write reduced costs;
6. write message;
7. produce deterministic JSON formatting.

Add tests using `tmp_path`.

------

# Part 8: Fixtures

Review or add small fixtures under:

```text
tests/fixtures/lp_small/
tests/fixtures/mip_small/
```

At minimum keep or add:

```text
tests/fixtures/lp_small/production.json
tests/fixtures/lp_small/diet.json
tests/fixtures/mip_small/knapsack.json
```

Add invalid fixtures only if they are useful and small. Otherwise, create invalid payloads inside tests.

Do not add large benchmark data.

------

# Part 9: Tests

Add or update tests under:

```text
tests/unit/
```

Suggested tests:

```text
test_model.py
test_expressions.py
test_canonical_form.py
test_json_reader.py
test_solution_writer.py
test_cli.py
```

The test suite should verify:

1. model construction;
2. duplicate variable rejection;
3. duplicate constraint rejection;
4. invalid bounds rejection;
5. unknown variable in constraint rejection;
6. unknown variable in objective rejection;
7. binary bounds validation;
8. expression arithmetic;
9. JSON reader behavior;
10. canonical form ordering and content;
11. solution writer output;
12. existing CLI tests still pass.

------

# Part 10: Examples

Update examples if needed:

```text
examples/lp/production_planning.py
examples/lp/diet_problem.py
examples/mip/knapsack.py
```

Examples should still build models successfully. They do not need to solve models yet.

------

# Part 11: Do Not Do

Do not implement tableau simplex.

Do not implement presolve.

Do not implement branch-and-bound.

Do not add external solver calls to native algorithm modules.

Do not introduce SciPy/HiGHS/GLPK as required dependencies.

Do not change the Apache-2.0 LICENSE.

Do not modify existing files under `tasks/codex/`.

Do not add generated outputs, cache files, virtual environments, or large datasets.

Do not force push.

------

# Part 12: Local Checks

Run:

```bash
python -m pip install -e ".[dev]"
pytest
python scripts/check_quality.py
python -m silo.cli.main --version
python -m silo.cli.main help
```

If the console script is available, also run:

```bash
silo --help
silo --version
```

Fix any errors.

------

# Part 13: Commit and Push

After all checks pass:

```bash
git status --short
git add .
git commit -m "Complete SILO model core"
git pull --rebase origin main
git push origin main
git status
git log --oneline -5
```

Do not use force push.

------

# Acceptance Criteria

This task is complete only if:

1. `Model.validate()` exists and catches core consistency errors.
2. Linear expression arithmetic is implemented and tested.
3. JSON reader loads small LP/MIP fixtures and validates models.
4. Solution writer has deterministic JSON tests.
5. Canonical form includes objective constant, variable bounds, and variable types.
6. Canonical form preserves variable and constraint order.
7. All new and existing tests pass.
8. `python scripts/check_quality.py` passes.
9. CLI help/version commands work.
10. No simplex, MIP search, cuts, decomposition, stochastic, robust, or native code is implemented.
11. Existing `tasks/codex/` files are not modified.
12. The task file itself is saved as `tasks/codex/20260519-03-01_model-core.md`.
13. The final commit is pushed to `origin/main`.
14. `git status` is clean after push.

```
这个任务完成后，SILO 的模型入口层就基本稳了。然后再进入 `20260519-04-01_tableau-mvp.md`，开始真正写 tableau simplex。
```