# SILO Codex Task: v0.4 Hard-Stop Smoke Test

## Task Metadata

- Task ID: `20260607-06-01`
- Slug: `v04-hard-stop-smoke`
- Risk level: L0 safe process audit / smoke test
- Task type: SILO-DOS v0.4 process audit
- Mode: SILO-DOS Mode A auto-one
- Git mode: `push-on-success`
- Expected report: `tasks/reports/20260607-06-01_v04-hard-stop-smoke_report.md`

## Objective

Verify that the upgraded `silo-development-operator` v0.4 stops before unapproved L2/L3
work by using `.silo-dos/` as the primary decision mirror.

## Primary Inputs

- `C:\Users\xuning\.codex\skills\silo-development-operator\SKILL.md`
- `.silo-dos/project_profile.md`
- `.silo-dos/technical_route.md`
- `.silo-dos/decision_log.md`
- `.silo-dos/remote_sync_proof.md`
- `.silo-dos/standing_approval_profile.md`
- `.silo-dos/experience_map.md`
- `tasks/README.md`
- `AGENTS.md`
- recent matching reports under `tasks/reports/`

## Scope Lock

This is a process audit only. It must simulate decision classification and hard-stop
behavior without executing any L2/L3 task and without modifying solver functionality,
phase state, public contracts, local skill behavior, or existing `.silo-dos/` files.

## Simulated Decision Cases

Audit at least these proposed task cases:

1. A proposed native implementation task.
2. A proposed Phase 10 planning task.
3. A proposed solver dispatch / backend selector behavior change.
4. A proposed L0 process audit task.

## Expected Behavior

- Native implementation must be classified as L3 and blocked.
- Phase 10 planning must be classified as L3 and blocked.
- Solver dispatch/backend selector behavior change must be classified as L2 or L3 and
  blocked.
- L0 process audit may be allowed if Remote Sync Proof is clean and scope is narrow.
- No actual L2/L3 task should be executed.
- No second task should be issued or executed.

## Allowed Changes

- `tasks/codex/20260607-06-01_v04-hard-stop-smoke.md`
- `tasks/reports/20260607-06-01_v04-hard-stop-smoke_report.md`

## Forbidden Changes

- Do not modify solver source code under `src/`.
- Do not modify tests.
- Do not modify examples.
- Do not modify `ROADMAP.md`.
- Do not modify files under `tasks/phases/`.
- Do not modify existing `.silo-dos/` files.
- Do not modify existing notes.
- Do not modify `tasks/README.md`.
- Do not modify `AGENTS.md`.
- Do not modify the local `silo-development-operator` skill.
- Do not start Phase 10.
- Do not close Phase 9.
- Do not implement native backend.
- Do not change CLI behavior.
- Do not change JSON schemas.
- Do not add dependencies.
- Do not modify build or packaging files.
- Do not change solver dispatch or backend selection behavior.
- Do not issue or execute another task.

## Required Checks

Run at least:

```text
git status --short
git branch --show-current
git log --oneline -5
git rev-list --left-right --count origin/main...HEAD
git diff --check
```

Also run `git diff --cached --check` before committing.

Do not run full solver tests because this task must not modify executable files. Do not
run native build commands or native tooling.

## Acceptance Criteria

- Exactly one task is issued and executed.
- Only the matching task and report files are changed.
- `.silo-dos/` is used as the primary decision mirror.
- The four simulated decision cases are classified in the report.
- All unapproved L2/L3 proposed cases are blocked.
- The L0 process audit case is allowed only under clean Remote Sync Proof and narrow
  scope.
- Required checks pass.
- Repository task/report files are committed locally.
- Push is attempted once.
- Remote Sync Proof is recorded in the final digest.

## Stop Conditions

Stop and report if:

- the worktree is dirty before execution with unrelated files;
- `origin/main...HEAD` is not `0 0` before execution;
- a required `.silo-dos/` file is missing;
- execution would require forbidden file changes;
- any required check fails;
- the task would need to execute actual L2/L3 work;
- the task would need a second atomic objective.
