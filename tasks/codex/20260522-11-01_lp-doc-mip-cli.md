# Codex Task: Fix Stale MIP CLI Wording in LP Solver Documentation

## Task Metadata

Task ID: 20260522-11-01
Task slug: lp-doc-mip-cli
Task type: documentation-consistency
Related phase: Phase 5 / MIP branch-and-bound documentation
Git mode: local-commit
Expected report path: tasks/reports/20260522-11-01_lp-doc-mip-cli_report.md

## Objective

Update `docs/lp_solver.md` to remove stale wording that says SILO does not expose a MIP CLI path.

The goal is only to make the LP solver documentation consistent with the current repository state, where SILO already exposes a separate `silo mip-solve` command for supported MIP JSON models.

## Pre-Issue Task ID Check

Before creating this task file or report, scan existing filenames under:

```text
tasks/codex/
tasks/reports/
```

If `20260522-11-01` already exists for a different slug, do not reuse it. Use the next available `TT` value and update the task ID, task filename, report filename, and metadata consistently.

## Context

`docs/lp_solver.md` currently states that the MIP layer has a small Python `BranchAndBoundSolver` API, but it also says that SILO still does not expose a MIP CLI path.

This is now stale because the project exposes a separate `silo mip-solve` command for supported MIP JSON models.

This task should update the wording without changing solver behavior.

## Scope Lock

This task is atomic.

Primary objective:

- Fix stale MIP CLI wording in `docs/lp_solver.md`.

Allowed changes:

- `docs/lp_solver.md`
- `tasks/reports/20260522-11-01_lp-doc-mip-cli_report.md`

Forbidden changes:

- Do not modify solver source code.
- Do not modify tests.
- Do not modify `tasks/README.md`.
- Do not modify `AGENTS.md`.
- Do not modify `CONTRIBUTING.md`.
- Do not modify existing files under `tasks/codex/`.
- Do not modify existing files under `tasks/phases/`.
- Do not modify `README.md` or `ROADMAP.md`.
- Do not push to GitHub unless explicitly instructed by the user.

Stop conditions:

- If the intended task ID collides with an existing task or report prefix, choose the next available `TT` and report the adjustment.
- If the documentation inconsistency appears to require code changes, stop and report instead.
- If unrelated repository changes are present before starting, inspect and report them before modifying files.

## Required Documentation Update

In `docs/lp_solver.md`, revise the stale sentence under the MIP relaxation boundary section.

The revised wording should communicate:

- LP backends still solve only continuous LP relaxations.
- MIP solving is exposed through the separate `silo mip-solve` command.
- `silo mip-solve` uses branch-and-bound and calls LP backends internally for relaxations.
- The LP solver documentation should not imply that tableau or revised simplex are themselves MIP algorithms.

Keep the change concise. Do not rewrite the entire document.

## Acceptance Criteria

This task is complete only if:

1. `docs/lp_solver.md` no longer says SILO lacks a MIP CLI path.
2. The document clearly distinguishes LP backends from the MIP CLI.
3. No solver source code is modified.
4. No tests are modified.
5. No existing task file is modified.
6. A report is created at the expected report path.

## Required Checks

Run at least:

```bash
git status --short
git diff --check
```

Do not run heavy solver tests for this documentation-only task.

## Report Requirements

Create the matching report under `tasks/reports/`.

The report must include:

```text
Task ID:
Objective:
Files changed:
Summary of documentation update:
Task ID pre-issue scan result:
Checks run:
Git status before:
Git status after:
Local commit hash:
Push attempted:
Issues or conflicts:
Next recommended atomic task:
```

## Git Instructions

Default Git mode for this task is:

```text
local-commit
```

After completing the update and report:

```bash
git add docs/lp_solver.md tasks/reports/20260522-11-01_lp-doc-mip-cli_report.md
git commit -m "docs(lp): clarify MIP CLI boundary"
```

Do not push unless the user explicitly requests it.

## Final Response

When finished, report only:

- whether `docs/lp_solver.md` was updated;
- whether the report was created;
- whether a local commit was created;
- whether push was skipped or completed;
- the next recommended atomic task.

这个小任务完成后，就可以正式开始写 `silo-development-operator` skill 了。
