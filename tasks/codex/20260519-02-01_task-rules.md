建议本次任务正式命名为：

```text
tasks/codex/20260519-02-01_task-rules.md
```

任务标题：

```markdown
# Codex Task 20260519-02-01: Organize Task Rules
```

commit message：

```text
Organize SILO task rules
```

下面这份可以直接发给 Codex。

~~~markdown
# Codex Task 20260519-02-01: Organize Task Rules

## Goal

Organize the `tasks/` directory of the SILO repository into a stable long-term task-management structure. This task is the first task that follows the new task-file naming convention.

The main purpose is to clearly distinguish:

1. long-term phase roadmap files;
2. issued Codex task prompts;
3. optional execution reports.

Also create a permanent rule file under `tasks/` so that all future task files follow the same naming, folder, and numbering rules.

Repository:

https://github.com/exoplanetX/silo

## Required Task File Name

This task itself must be saved as:

```text
tasks/codex/20260519-02-01_task-rules.md
~~~

If the file does not exist, create it and put this full task prompt into it.

This is a one-time migration task. During this task only, existing task files may be moved or renamed to conform to the new rules. After this task is completed, issued files under `tasks/codex/` must be treated as immutable records.

------

# Part 1: Create the Standard Tasks Directory Structure

Create or normalize the following structure:

```text
tasks/
  README.md

  phases/
    phase_00_project_scaffold.md
    phase_01_model_core.md
    phase_02_tableau_simplex.md
    phase_03_revised_simplex.md
    phase_04_presolve_scaling.md
    phase_05_branch_and_bound.md
    phase_06_cut_callbacks.md
    phase_07_decomposition.md
    phase_08_stochastic_robust.md
    phase_09_native_backend.md

  codex/
    20260519-01-01_phase-num.md
    20260519-02-01_task-rules.md

  reports/
    .gitkeep
```

If the current repository already has phase files directly under `tasks/`, move them into `tasks/phases/`.

If the previous Codex task for phase numbering exists under another name, rename it to:

```text
tasks/codex/20260519-01-01_phase-num.md
```

If the previous phase-numbering prompt does not exist as a file, do not invent content. Only move/rename existing files.

------

# Part 2: Create `tasks/README.md`

Create or update:

```text
tasks/README.md
```

This file is the permanent rule file for all task-related files.

It must explain:

## 1. Purpose of `tasks/`

The `tasks/` directory is not a general notes folder. It is used to manage long-term phase plans, issued Codex task prompts, and optional execution reports.

## 2. Folder Roles

Use exactly these roles:

```text
tasks/phases/
```

Long-term phase roadmap files. These describe solver-development phases, goals, scope, expected files, testing requirements, and acceptance criteria. They are not one-off Codex commands.

```text
tasks/codex/
```

Issued Codex task prompts. These are the task instructions sent to Codex for execution.

Files under `tasks/codex/` are immutable after creation. Coding agents may read them, but must not edit, rename, delete, or move existing files unless the user explicitly asks for task-file maintenance.

```text
tasks/reports/
```

Optional execution reports. If Codex or another agent needs to record what was done, what tests passed, or what failed, the report must be created here instead of modifying the issued task file.

## 3. Naming Rules

For long-term phase files:

```text
tasks/phases/phase_XX_short-name.md
```

Examples:

```text
tasks/phases/phase_01_model_core.md
tasks/phases/phase_02_tableau_simplex.md
```

For issued Codex task files:

```text
tasks/codex/YYYYMMDD-TT-RR_slug.md
```

Where:

```text
YYYYMMDD = date
TT       = task block number on that date, fixed two digits
RR       = revision number of that task block, fixed two digits
slug     = short kebab-case task label, preferably 1–3 words
```

Examples:

```text
tasks/codex/20260519-01-01_phase-num.md
tasks/codex/20260519-02-01_task-rules.md
tasks/codex/20260519-03-01_model-core.md
tasks/codex/20260519-03-02_model-core.md
```

Meaning:

```text
20260519-03-01_model-core.md
```

means the first version of the third task block issued on 2026-05-19.

For execution reports:

```text
tasks/reports/YYYYMMDD-TT-RR_slug_report.md
```

Example:

```text
tasks/reports/20260519-02-01_task-rules_report.md
```

## 4. Revision Rule

Do not overwrite an issued Codex task file to revise it.

If a task needs revision, create a new file with an incremented `RR` value.

Example:

```text
tasks/codex/20260519-03-01_model-core.md
tasks/codex/20260519-03-02_model-core.md
```

## 5. Rule Change Rule

If the task system itself needs to change in the future, `tasks/README.md` must be updated first or in the same commit.

No future task-directory restructuring should be done without updating this rule file.

------

# Part 3: Update `AGENTS.md`

Update `AGENTS.md` to include the following rules:

```text
Task Management Rules

- Long-term phase plans belong under tasks/phases/.
- Issued Codex task prompts belong under tasks/codex/.
- Optional execution reports belong under tasks/reports/.
- Files under tasks/codex/ are immutable after creation. Coding agents may read them, but must not edit, rename, delete, or move existing files under tasks/codex/ unless the user explicitly asks for task-file maintenance.
- If a task needs revision, create a new tasks/codex/YYYYMMDD-TT-RR_slug.md file with an incremented revision number.
- If an execution report is needed, create it under tasks/reports/ instead of modifying the issued task file.
- If the task naming or folder rules need to change, update tasks/README.md first or in the same commit.
```

Keep the existing AGENTS.md solver-development rules. Do not remove them.

------

# Part 4: Preserve Current Phase Numbering

Ensure the phase files under `tasks/phases/` follow this phase convention:

```text
Phase 0: Project Scaffold
Phase 1: Model Core and Canonicalization
Phase 2: Tableau Simplex
Phase 3: Revised Simplex and Basis Reoptimization
Phase 4: Presolve, Scaling, and Numerical Diagnostics
Phase 5: MIP Branch-and-Bound
Phase 6: Cut Generation and Callbacks
Phase 7: Decomposition Layer
Phase 8: Stochastic and Robust Optimization Extensions
Phase 9: Native Backend
```

Do not change the solver roadmap substance unless needed to fix naming or folder references.

------

# Part 5: Do Not Do

Do not implement simplex.

Do not implement model-core functionality.

Do not change package architecture.

Do not modify the Apache-2.0 LICENSE.

Do not add generated files, cache files, virtual environments, or outputs.

Do not force push.

Do not rewrite Git history.

------

# Part 6: Local Checks

Run:

```bash
python -m pip install -e ".[dev]"
pytest
python scripts/check_quality.py
python -m silo.cli.main --version
python -m silo.cli.main help
```

If the console script is available, also run:

```bash
silo --help
silo --version
```

Fix only errors related to file moves, imports, stale references, or documentation links.

------

# Part 7: Commit and Push

Check status:

```bash
git status --short
```

Commit:

```bash
git add .
git commit -m "Organize SILO task rules"
```

Synchronize and push:

```bash
git pull --rebase origin main
git push origin main
```

Final verification:

```bash
git status
git log --oneline -5
```

------

# Acceptance Criteria

This task is complete only if:

1. `tasks/README.md` exists and clearly defines the task directory rules.
2. `tasks/phases/` contains long-term phase files.
3. `tasks/codex/` contains issued Codex task prompts.
4. This task is saved as `tasks/codex/20260519-02-01_task-rules.md`.
5. Previous phase-numbering task, if present, is named `tasks/codex/20260519-01-01_phase-num.md`.
6. `tasks/reports/` exists with `.gitkeep`.
7. `AGENTS.md` includes the task-management immutability rules.
8. Phase numbering remains consistent from Phase 0 to Phase 9.
9. `pytest` passes.
10. `python scripts/check_quality.py` passes.
11. CLI help/version commands work.
12. No generated files, cache directories, or large outputs are committed.
13. The final commit is pushed to `origin/main`.
14. `git status` is clean after push.

```
这个任务完成后，`tasks/` 就正式变成三层结构：

```text
tasks/phases/   长期路线
tasks/codex/    发布给 Codex 的任务原文
tasks/reports/  执行报告
```

以后你只需要记住：**发任务放 `tasks/codex/`，看路线放 `tasks/phases/`，查规则看 `tasks/README.md`。**