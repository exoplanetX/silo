## 

任务文件名：

```text
tasks/codex/20260519-04-01_tableau-mvp.md
```

任务标题：

```markdown
# Codex Task 20260519-04-01: Implement Tableau Simplex MVP
```

commit message：

```text
Implement tableau simplex MVP
```

这次任务只做：

```text
1. 标准 max <= LP 的 slack-form 转换
2. tableau 数据结构
3. deterministic pivot rule
4. ratio test
5. optimal / unbounded 状态判断
6. 返回 objective value 和 primal solution
7. 小型 known-solution tests
```

暂时不做：

```text
>= 约束
= 约束
Phase I
dual values
reduced costs 完整输出
revised simplex
presolve
MIP
```

也就是说，这次目标不是“完整 simplex”，而是：

> **先让 SILO 第一次真正 solve 一个小 LP。**

